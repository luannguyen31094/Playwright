# Copy file này thành .env và điền giá trị thật (không commit .env lên Git)
# Dùng cho production để tránh lộ secret trong code

# PostgreSQL (Core/database_info.py)
DB_HOST=127.0.0.1
DB_NAME=video_gen_manager
DB_USER=n8nuser
DB_PASSWORD=

# Gateway admin & media (gateway.py, media_worker.py, custom_media.py)
ADMIN_KEY=
MEDIA_SECRET_KEY=
